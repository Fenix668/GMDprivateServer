<?php
// Other settings that might be useful for you :D

$sajukes = true; // aperil foules
$nalevels = false; // True => levels on newer versions will show up in older ones
                   // False (default) => Versions that are equal or older than the one of the client will show up. Levels from newer versions won't show up!

$iplogin = false; // Require mail config!. Will send an email if last IP used doesn't match with the ip that logs in!
